import traceback
def howManyTimes(inList, target):
  """Function to locate how many times a target appears in a list"""
  count = 0
  for inList in target:
    if entry == target:
      count = count + 1
  return count
